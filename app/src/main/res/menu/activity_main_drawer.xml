<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- TODO Change icon colors depending on technology-->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_camera"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:title="@string/nav_change_technology" />
        <item
            android:id="@+id/nav_gallery"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:title="@string/nav_history" />
        <item
            android:id="@+id/nav_slideshow"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:title="Hide images" />

        <item
            android:id="@+id/nav_connect"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:title="Hide images" />
    </group>

    <item android:title="Words">
        <menu>
            <item
                android:id="@+id/nav_rate"
                android:icon="@android:drawable/ic_btn_speak_now"
                android:title="Connect with Facebook" />
            <item
                android:id="@+id/nav_about"
                android:icon="@android:drawable/ic_btn_speak_now"
                android:title="Like app" />
            <item
                android:id="@+id/nav_help"
                android:icon="@android:drawable/ic_btn_speak_now"
                android:title="Log out" />
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_search_black_48dp"
                android:title="Log out" />
        </menu>
    </item>

</menu>
